{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -e \"require('child_process').execSync('parcel ' + (process.argv[1] || 'app.js') + ' --dist-dir dev', { stdio: 'inherit' })\""
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  },
  "dependencies": {
    "parcel": "^2.12.0"
  }
}
